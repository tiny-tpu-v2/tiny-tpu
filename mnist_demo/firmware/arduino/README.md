# Arduino

This folder groups the Arduino sketches associated with the MNIST demo.

- `arduino-blink/`: simple sanity-check sketch.
- `arduino_touch_sender/`: sketch used to send demo input data toward the host flow.
