# Arduino Blink

This folder contains a minimal blink sketch used to verify that the attached Arduino is reachable and programmable.

It is useful for hardware sanity checks before testing the full MNIST input flow.
