# Arduino Touch Sender

This folder contains the Arduino sketch that streams input activity for the MNIST demo pipeline.

Use it when exercising the live handwritten-input path instead of a simple device bring-up check.
