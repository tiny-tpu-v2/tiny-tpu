# Model Data

This folder contains quantized model assets consumed by the MNIST FPGA design, testbenches, and preview tools.

- `generated/`: locally regenerated outputs from training.
- `reference/`: checked-in baseline assets used for the default demo flow.
