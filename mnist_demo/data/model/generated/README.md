# Generated Model

This folder stores outputs written by `python3 train_mnist.py`.

Expect regenerated `.memh` weights, sample inputs, expected outputs, and a `summary.json` file for the most recent training run.
