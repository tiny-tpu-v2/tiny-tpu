# Simulation Outputs

This folder stores generated ModelSim outputs for the MNIST testbenches.

Each `modelsim_*` subfolder corresponds to one runner script in `mnist_demo/sim/` and contains transcripts, local configuration, and often a compiled `work/` library.
