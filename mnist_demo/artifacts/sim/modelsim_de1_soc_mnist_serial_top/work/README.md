# Work Library

This folder contains the generated ModelSim `work` library for the `de1_soc_mnist_serial_top` simulation run.

It can be recreated by rerunning the matching script in `mnist_demo/sim/`.
