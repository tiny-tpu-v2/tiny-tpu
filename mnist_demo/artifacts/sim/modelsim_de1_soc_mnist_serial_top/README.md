# ModelSim DE1-SoC MNIST Serial Top

This folder stores generated ModelSim outputs for `run_de1_soc_mnist_serial_top_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the board-level serial top testbench.
