# ModelSim MNIST Serial Classifier Full

This folder stores generated ModelSim outputs for `run_mnist_serial_classifier_full_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the fuller serial classifier regression.
