# ModelSim MNIST JTAG Parity

This folder stores generated ModelSim outputs for `run_mnist_jtag_parity_regression.sh`.

It typically contains the regression transcript and any supporting files emitted while checking parity between the JTAG and reference flows.
