# ModelSim MNIST Serial Classifier

This folder stores generated ModelSim outputs for `run_mnist_serial_classifier_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the serial classifier testbench.
