# ModelSim UART Frame Receiver

This folder stores generated ModelSim outputs for `run_uart_frame_receiver_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the UART frame receiver testbench.
