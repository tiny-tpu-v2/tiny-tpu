# ModelSim UART RX

This folder stores generated ModelSim outputs for `run_uart_rx_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the UART receiver testbench.
