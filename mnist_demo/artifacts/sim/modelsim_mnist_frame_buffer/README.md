# ModelSim MNIST Frame Buffer

This folder stores generated ModelSim outputs for `run_mnist_frame_buffer_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the frame buffer testbench.
