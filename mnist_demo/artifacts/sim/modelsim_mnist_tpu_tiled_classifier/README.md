# ModelSim MNIST TPU Tiled Classifier

This folder stores generated ModelSim outputs for `run_mnist_tpu_tiled_classifier_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the tiled classifier testbench.
