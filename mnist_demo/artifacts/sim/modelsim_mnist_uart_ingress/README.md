# ModelSim MNIST UART Ingress

This folder stores generated ModelSim outputs for `run_mnist_uart_ingress_tb.sh`.

Expect a transcript, local ModelSim configuration, and the compiled `work/` library for the UART ingress testbench.
