# Synthetic Handdrawn

This folder stores outputs from the synthetic handwritten benchmark flow.

- `results.json`: aggregate benchmark metrics.
- `previews/`: rendered sample previews and per-sample prediction dumps.
