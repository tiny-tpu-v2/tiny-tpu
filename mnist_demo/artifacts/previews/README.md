# Previews

This folder holds generated visualizations for model assets and dataset samples.

- `bitmask/`: previews derived from `.bits`, `.bin`, and runtime frame data.
- `dataset/`: rendered MNIST dataset examples and indexes.
