# Submodules

This folder contains vendor-generated HDL submodules referenced by the synthesized `mnist_jtag_bridge` IP block.

These files are generated integration collateral rather than handwritten project RTL.
