# Submodules

This folder contains vendor-generated HDL submodules used by the synthesized `probe_jtag_bridge` IP block.

Expect bridge adapters, FIFO helpers, reset logic, and other generated support modules.
